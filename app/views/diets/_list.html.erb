<%= render Elements::TableComponent.new(
  row_fields: [
    { field: :name, link: ->(label, object) { link_to(label, diet_path(object)) }},
    { field: :description },
  ],
  row_objects: diets,
  table_alias: diets.model.to_s.downcase,
  # row_actions: {}
)%>