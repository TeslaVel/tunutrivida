
<div class="row w-400px bg-secondary-200 rounded-lg p-3 m-auto">

  <%= simple_form_for(entry) do |f| %>

    <%= notice %>
    <%= f.error_notification %>
    <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

    <div class="form-group">
      <%= f.label :image %>
      <%= f.file_field :image %>
    </div>
    <div class="form-group">
      <%= f.input :description, label: t('.description'), input_html: { class: "form-control" } %>
    </div>

    <div class="form-group">
      <%= f.input :entry_type, collection: Entry::ENTRY_TYPES.map { |type| [type.to_s, type] }, input_html: { class: 'form-control' } %>

    </div>

    <div class="d-flex justify-content-center align-items-center form-group gap-5 ">
      <%= f.button :submit, action_button, class: "btn btn-primary mr-10" %>
      <%= link_to 'back', entries_path,  class: "ml-10" %>
    </div>

  <% end %>
</div>
