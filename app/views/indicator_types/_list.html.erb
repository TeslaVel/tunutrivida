<%= render Elements::TableComponent.new(
  row_objects: indicator_types,
  table_alias: indicator_types.model.name.underscore,
  row_fields: [
    { field: :name, link: ->(label, object) { link_to(label, indicator_type_path(object)) }},
    { field: :description },
  ],
  row_actions: {}
) %>