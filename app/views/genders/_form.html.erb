<div class="col-xs-12 col-sm-10 col-md-8 col-lg-6 mx-auto">
  <%= render Forms::FormComponent.new(
      scope_instance: gender,
      is_row: true,
      data_type: { type: :json },
      html_options: { id:"gender_form_id", class: 'form', remote: false},
      action_buttons: {
      },
      fields: [
        { type: 'text', name: :name, label: :name, value: gender.name },
        { type: 'text', name: :description, label: :description, value: gender.description },
      ]
  ) %>
</div>